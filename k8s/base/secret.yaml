apiVersion: v1
kind: Secret
metadata:
  name: geotrack-secrets
  namespace: geotrack
  labels:
    app.kubernetes.io/part-of: geotrack
type: Opaque
stringData:
  # Replace these in overlays or use sealed-secrets / external-secrets
  QUARKUS_DATASOURCE_PASSWORD: "changeme"
  AISSTREAM_API_KEY: "changeme"
  POSTGRES_USER: "geotrack"
  POSTGRES_PASSWORD: "changeme"
  POSTGRES_DB: "geotrack"
