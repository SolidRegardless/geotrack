package com.geotrack.common.model;

/**
 * Geofence behaviour type.
 * INCLUSION — alert when asset LEAVES the zone.
 * EXCLUSION — alert when asset ENTERS the zone.
 */
public enum FenceType {
    INCLUSION,
    EXCLUSION
}
